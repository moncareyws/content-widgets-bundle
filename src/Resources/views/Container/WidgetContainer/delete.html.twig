    {% extends '@SamuelmcFdnContentWidgets/Shared/page.html.twig' %}

{% block content %}

    <h1>Delete Container\WidgetContainer</h1>

    <p>Are you sure you want to delete this container\WidgetContainer?</p>

    {{ form_start(form) }}

        {{ form_widget(form) }}

        <button type="submit" class="button">
            <i class="fa fa-trash"></i>
            Delete
        </button>
        <a class="button" href="{{ app.request.headers.get('referer') }}">
            <i class="fa fa-times"></i>
            Cancel
        </a>
    {{ form_end(form) }}

{% endblock %}
