{% if config.edit %}
    {% include '@content_widgets/widget_templates/base/widget_container_editable.html.twig' %}
{% else %}
<div id="{{ container.name }}-widgetcontainer" class="widgetcontainer {{ container.name }}-widgetcontainer">
    {% endif %}

    {% for widget in container.widgets %}
        {{ render_widget(widget) }}
    {% endfor %}

</div>

